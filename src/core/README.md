# core

Business logic and domain rules live here, independent of React, the database
and any external provider. Phase 0 ships only this note — real domain modules
(leads, appointments, availability, automation rules) arrive in Phase 1.
