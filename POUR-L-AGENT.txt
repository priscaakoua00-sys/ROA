CORRECTIF COMPLET + NOUVEAU DESIGN VOERTUIGEN
branche : claude/roa-ui-quality-upgrade-gn974g  (repo ROA)
=========================================================

CONTEXTE : le deploiement Vercel echouait ("two parallel pages ... customers").
Une fusion avait ramene les anciens dossiers de routes en double ET supprime
plusieurs dependances + la correction du chat Robin. Ce paquet repare tout
ET ajoute le nouveau design de l'ecran Voertuigen. Deja teste en local :
typecheck, lint, 47 tests et npm run build passent tous au vert.

--- PARTIE A : fichiers a placer (mêmes chemins) ---
  package.json                                     (remplace)
  package-lock.json                                (remplace)
  src/components/robin-chat.tsx                     (remplace)
  src/components/vehicles/car-illustration.tsx      (nouveau)
  src/components/vehicles/vehicle-card.tsx          (nouveau)
  src/app/[locale]/(app)/vehicles/page.tsx          (remplace : nouveau design en grille)

Puis installer les dependances :  npm install

--- PARTIE B : supprimer les 11 anciens dossiers de routes en double ---
Une seule commande git a la racine du depot, sur la branche :

  git rm -r "src/app/[locale]/agenda" "src/app/[locale]/automations" \
    "src/app/[locale]/customers" "src/app/[locale]/dashboard" \
    "src/app/[locale]/knowledge" "src/app/[locale]/leads" \
    "src/app/[locale]/notifications" "src/app/[locale]/settings" \
    "src/app/[locale]/team" "src/app/[locale]/vehicles" \
    "src/app/[locale]/work-orders"

NE PAS supprimer : le dossier (app), ni login, signup, onboarding,
forgot-password, reset-password, request, auth, page.tsx, layout.tsx,
error.tsx, not-found.tsx.

--- FINIR ---
  npm run build   (doit passer au vert)
  puis commit et push.

IMPORTANT : ne pas re-fusionner main dans cette branche apres coup, sinon les
anciens dossiers plats reviennent et cassent a nouveau le build.
